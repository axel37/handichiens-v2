{% extends 'disposition_standard.html.twig' %}

{% block title %}Handi'chiens - Administrateur{% endblock %}

{% block contenu %}

    <p>Vous êtes connecté en tant qu'administrateur.</p>

    <p><a href="{{ path('app_deconnexion') }}">Se déconnecter.</a></p>

    <pre>User :</pre>
    {{ dump(app.user) }}

    {{component('carte', { type: "defaut", href: "/", textes: 'Test'})}}

    {{component('carte', { type: "utilisateur", href: "/", entite: app.user})}}

{% endblock %}
